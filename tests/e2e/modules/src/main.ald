import (
    io,

    ~/math,
)

pub fn main() {
    let result = math.answer({
        value: 40,
        note: "from an indexed module",
        meta: { increment: 2, enabled: true },
    })
    io.print(result)
}
