import io
enum Maybe[a] {
    Nothing,
    Just(a),
}

fn unwrap(value) {
    match value {
        Maybe::Just(number) => number,
        Maybe::Nothing => 0,
    }
}

pub fn main() {
    io.print(unwrap(Maybe::Just(42)))
}
