import task
test "synchronous typed failure" {
    Err(:sync_failure)
}

test "asynchronous typed failure" {
    async {
        task.sleep(0).await
        Err(:async_failure)
    }
}

test "asynchronous defect" {
    async {
        task.sleep(0).await
        assert false
    }
}

test "later asynchronous success" {
    async {
        task.sleep(0).await
        assert true
    }
}
