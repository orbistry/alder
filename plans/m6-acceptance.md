# Full M6 acceptance ledger

Active goal: finish `plans/m6-web.md`, not only its existing counter slice.
Unchecked items are not proven complete. Record implementation paths and actual
test/manual evidence next to each gate as it lands. Browser, standalone, and
local Cloudflare verification are distinct gates. No commit or push.

## Usable application workflow

- [x] A clean app with `src/routes/+page.ald` needs no handwritten JS bootstrap. Created a fresh temporary project containing only `alder.jsonc` and the exact counter page from `docs/web-development.md`; real Chrome on port 4350 verified SSR, generated bootstrap, existing-node identity, and native clicks.
- [x] `alder dev` prints a reachable localhost URL and serves SSR plus client assets. Clean source-only `web-full` copies ran on standalone 4342 and Miniflare 4343; both were opened and exercised in Chrome.
- [x] Real browser hydration attaches interactive components to existing SSR nodes. `tools/web-full-browser-check.mjs` passes both targets, including original greeting/count node identity, no initial resource request, and shared-store updates.
- [x] Editing components/layouts/routes updates the browser; new/deleted routes are discovered. `tools/web-hmr-browser-check.mjs` passed both clean copies: layout/page edits, added-route navigation/history, then route removal returning 404 without a document reload.
- [x] Compile errors surface actionable source diagnostics and recover without restarting dev. An undefined page name produced terminal file/line diagnostics and browser overlay; correcting it resumed HMR with local/store values retained on both targets. Direct failed-replacement tests retain the old tree and handlers.
- [x] Compatible HMR preserves component signal and module-store state; incompatible changes have documented fallback. Both Chrome runs retained shared count 2 and local command count 2 through layout edits. Adding a state cell reset local count to 0, retained shared count 2, and printed the compatibility reason without reload. `tools/web-reconnect-browser-check.mjs` passes document/client build races and missed-event recovery on both targets.
- [x] Shutdown releases watcher, listener, runtime, child processes, and ports. Both dev processes exited 0 on Ctrl-C; immediate HTTP probes refused connections, and both ports were successfully reused. Cloudflare bridge also has separately recorded local runtime disposal checks.

## Components, typing, and rendering

- [x] Checked-in WHATWG HTML vocabulary (115 elements, 31 global attributes), per-element types and ARIA/data support. `tools/html-schema-source.html.gz` pins the authoritative input; its SHA-256 is recorded in generated Rust. `python3 tools/generate-html-schema.py --check` passes offline. This covers implemented structural content checks, not every conditional WHATWG rule or opaque cross-component Html shape.
- [x] Unknown tags/attrs, wrong values, invalid nesting, and event types have source-aware diagnostics; dashed custom elements work. Driver unknown-element/attribute, wrong-attribute/event, nested-anchor/button/form, interactive-descendant and checkbox-input-event snapshots pass and were reviewed. Solver `web_schema_*`, `input_event_fields_require_a_known_text_control`, and `interactive_and_form_nesting_checks_cross_literal_regions` cover positive/custom cases and directive traversal.
- [x] Component composition, optional/required typed props, and typed children work across module boundaries. Compiler regressions: `imported_components_render_through_the_typed_html_module`, `compiled_composition_directives_and_keyed_rows_reuse_hydrated_nodes`; solver `web_component_props_children_and_optional_field_evidence` checks required/optional fields and children evidence.
- [x] Synchronous and Task event handlers receive appropriate typed browser events. Compiler `event_objects_tasks_and_component_tags_compile`; kernel `synchronous_and_task_event_handlers_receive_the_native_event` executes both kinds and verifies disposal.
- [x] Compile-time dependencies, derived values, writable state, setup-once behavior, and independent owners remain correct. Compiler counter/setup regressions; kernel memo/diamond tests; transitive private imported-store captures survive interface cache/copy and alter fingerprints. Concrete annotated component params and simple reactive let bindings remain required.
- [x] Reactive `@if` and `@match` own/update/dispose branch regions. Compiler composition test and kernel `keyed_match_payload_updates_keep_current_handlers_and_release_removed_subtrees` verify retained payloads, changed-arm setup, and listener/subscription removal.
- [x] Keyed `@for` preserves nodes and child state across reorder; add/remove/empty paths clean up. Kernel `keyed_regions_hydrate_reorder_refresh_and_dispose` and `hot_updates_restore_only_compatible_live_writable_cells_before_memos` cover node and keyed local-state identity.
- [x] Expanded component/directive SSR, escaping, strict hydration identity/mismatch handling, and disposal have direct tests. Driver web suite plus kernel failed-mount/hydration, void/RCDATA/template, leading-textarea-newline, and nested-provider async-SSR regressions. Real-browser application wiring is verified separately above.
- [x] Async resources participate in SSR, hydration-data reuse, refresh, cancellation, and cleanup. Compiler resource pipeline test and kernel stale-fiber/typed-failure, abort-finalizer, hydrated query invalidation, and incompatible-HMR-resource-payload regressions. Resources require direct owned component/directive lets; synchronous SSR rejects them.
- [x] Hydration transport round-trips supported Alder values (including enums, nested options, Map/Set/BigInt and date values) safely. `alder-kernel` transport suite covers graph identity/cycles, prototype/script safety, all token shapes and unreachable entries, nonexecuting rejection of accessors/custom containers/sparse arrays, and exact encode/decode node/depth/traversal budget boundaries (8 tests).

## Filesystem routes and data

- [x] Discovery covers pages, layouts, server companions, endpoints, errors, hooks, and remote modules. `web_routes::tests::discovers_all_conventions_and_inherits_layouts_and_errors`, `discovery_is_input_order_independent`, and `duplicate_hooks_rejected` execute the discovery pass directly.
- [x] Route params, precedence, conflicts, malformed paths, and URL encoding are checked deterministically. The 20-test `web_routes::tests` suite covers static/required/optional/rest precedence, suffix backtracking, duplicate source URIs, malformed conventions, Unicode/reserved-character round trips, invalid UTF-8/percent/separator rejection, and optional-param ambiguity. `runtime_matching_and_href_agree_with_compiler_route_algorithms` computes expected results with the real Rust matcher/href generator and executes the kernel equivalents in V8 across the same fixture matrix, including grouped paths, rest/static suffixes, optional backtracking, empty/dot/invalid UTF-8 paths, RFC3986 escaping, unknown/unsafe parameters, and generated-link round trips.
- [x] Generated typed Routes and href construction reject incorrect params. `generated_routes_compile_and_check_link_parameter_records` compiles the generated module and rejects missing/wrong params; `generated_route_links_preserve_identity_and_parameter_contracts` and the href tests cover canonical identity and runtime path construction.
- [x] Generated LoadEvent params and merged PageData reflect page and parent layout load results. `generated_load_event_checks_route_params_before_inference`, `nonexistent_load_parameter_is_rejected`, `page_data_is_solved_before_component_in_same_module`, `page_data_merges_nested_layout_and_server_and_universal_loads`, and `layout_params_do_not_include_descendant_params` pass through the staged compiler; the route suite also merges two ancestor layouts with server/universal loads.
- [x] Universal layout/page loads execute in the browser during client navigation, retaining trusted server-load dependencies. `browser_navigation_replays_universal_loads_with_trusted_server_stages` asserts initial SSR node reuse without duplicate loads, server-stage/browser-stage merge order, server-only child dependencies, and cached prerender replay. `browser_universal_load_failures_and_superseded_tasks_have_owned_cleanup` checks typed/unexpected boundaries, reporting, cancellation/finalizers, stale-history prevention, and disposal. `full_data_hmr_requests_keep_ssr_resources_but_navigation_defers_them` preserves the full SSR resource payload for HMR while navigation loads resources in browser owners. Necessary server reevaluation of universal ancestors before downstream server loads is documented in `docs/web-internals.md`; no client data is trusted as server-load input.
- [x] Nested layouts, load ordering/data propagation, and error boundaries work in SSR and navigation. Kernel `application_ssr_loads_layouts_and_serializes_client_data` checks layout-before-page execution, nested HTML, and navigation-data propagation; `error_boundaries_preserve_expected_payloads_and_hide_unexpected_secrets` checks typed expected payloads versus sanitized unexpected failures. Compiler `error_components_receive_contextual_typed_error_union` checks the contextual PageError alias. Real-browser navigation remains a separate gate below.
- [x] Client navigation, history/popstate, route errors, and cleanup are verified in a real browser. Full-example and HMR scripts pass on both runtimes. Additional browser checks prove universal layout/page loads observe browser store 2 while a child server load receives trusted server ancestor value 1; direct SSR starts at 1, history preserves browser state, and no page errors occur. CSR-disabled routes perform full navigation without scripts; SSR-disabled routes mount interactively from empty SSR containers on direct and client navigation.
- [x] JSON/API endpoints use typed request/response contracts and correct HTTP methods/statuses. Driver `hooks_endpoints_validate_concrete_params_and_native_response` rejects non-Response endpoints; `compiled_http_facade_preserves_native_objects_options_and_json_evidence` executes the compiled HTTP facade and `http_response_and_request_types_are_not_interchangeable` checks native type identity. Kernel `http_endpoints_options_and_invalid_paths_have_explicit_behavior` checks endpoint dispatch, 405, 404, malformed-path 400, and trailing-slash 308 responses.
- [x] Exported prerender/ssr/csr/trailingSlash options inherit and reject incompatible combinations. `page_options_inherit_and_static_entries_generate_encoded_paths`, `empty_and_async_entries_have_contextual_params_types`, and `page_option_contract_errors_are_diagnostics` exercise source AST extraction, inheritance, nominal TrailingSlash, contextual entries typing, and rejected contracts. Route option tests also reject ssr=false/csr=false, missing dynamic entries, and duplicate paths.
- [x] Build-time prerender emits static routes and enumerated dynamic entries. `compiled_async_entries_prerender_through_structured_runtime_sink` compiles real Alder entries/load/component code, emits Oxc entries/validators, bundles with Rolldown, and executes via embedded V8: encoded sorted paths and rendered HTML are asserted. Kernel `prerender_evaluates_entries_once_and_serves_built_html_and_navigation_data` checks cached HTML/navigation data and duplicate rejection; runtime `build_report` tests require exactly one structured result independently of console output. CLI artifact/staging and platform evidence are separate below.

## Server boundaries and application lifecycle

- [x] `*.remote.ald` generates typed client HTTP stubs and server dispatch, preserving Result errors. Driver remote signature/stub/wire-schema tests, kernel malformed/origin/size/result dispatch tests, and actual full-example query/command responses pass. Produced browser modules contain generated stubs, not server implementations.
- [x] Query caching, commands/invalidation/refresh have a documented explicit contract and tests. Kernel query-key/module invalidation tests and `navigation_cache_reset_rejects_late_queries_from_the_previous_page` pass; Chrome checks two explicit refresh requests and command-triggered query refresh. `docs/web.md` specifies page cache bounds, bypass, invalidation, and no cross-request server cache.
- [x] Server actions and the M6 form/resource workflow work end to end without implementing unrelated schema/data milestones. Both browser runtimes submit a one-character name and display the typed validation error, then submit a valid name and display the hook-provided viewer/result without navigation. Form capture is synchronous; M8 schema-generated components are explicitly deferred in `docs/web-actions.md`.
- [x] Client reachability rejects server modules/bindings/secrets with an explanatory source path; produced client artifacts are inspected. Driver browser-root/transitive binding and aliased server-companion regressions cover page/layout/error/client hooks; a real Rolldown artifact excludes remote/load/hook sentinels and host bindings. `examples/web-full` client output was inspected separately. Compiler-derived hydration store allowlists stop at remote stubs; kernel SSR/navigation tests omit private initialized stores while restoring event-only shared stores.
- [x] Server handle/handleError/handleFetch hooks cover pages, loads, endpoints, actions, and remotes. Driver `hooks_accept_generic_request_params_and_typed_fetch_continuations`/`hooks_reject_route_specific_params_and_wrong_effects`; kernel concurrent load context, action/remote context, fetch interception and sanitized error tests pass. New `endpoint_action_remote_hooks_intercept_fetch_and_report_each_defect_once` executes success and unexpected-error paths for all three dispatch kinds, checking suspended fetch interception, exactly-once reporting and provider cleanup. Browser/manual evidence remains separate.
- [x] Client init/error hooks are wired. Driver `hooks_client_init_and_reporting_accept_only_unit_effects`; kernel `client_init_and_navigation_failures_reach_the_typed_reporting_hook`, `client_event_and_startup_defects_use_owner_scoped_error_hooks`, and `client_resource_defects_report_but_expected_failures_remain_values` pass. They verify sync/Task init, navigation/abort behavior, initial-render and sync/Task event defects, disposed-owner isolation, and defect-versus-typed-resource-error reporting. These are direct runtime/DOM-shim tests, not real-browser hook coverage.
- [x] Existing provider/fiber context propagates through request handling without claiming the deferred DI guarantees. Kernel `server_hooks_keep_request_context_through_suspending_loads` runs concurrent requests across suspension and asserts separate context plus cleanup; `remotes_validate_requests_and_results_inside_request_hooks`, `action_dispatch_retains_route_url_params_and_typed_result_errors`, and `fetch_hooks_intercept_std_fetch_and_preserve_request_context_and_result` extend evidence to remote/action/fetch dispatch. These exercise existing runtime providers, not deferred static DI checks.
- [x] Module stores are isolated per server request and singleton/reactive in the browser; concurrency tests prove no cross-request leakage. Kernel `application_stores_are_isolated_across_suspension_and_serialized_after_ssr` and provider/SSR concurrency tests pass. Full browser scripts preserve the singleton across navigation while fresh remote/action/health requests see separate server values. Compiler-filtered hydration omits private server stores.

## Runtime, build, and deployment

- [x] Public asset byte handling, MIME/HEAD/query-path responses, reserved paths, and symlink rejection have direct tests. CLI `public_bytes_types_and_reserved_namespace`/`public_symlinks_are_rejected_without_reading_targets`; kernel `public_assets_preserve_binary_bytes_head_and_query_paths` executes native byte responses and rejects stale build-asset fallback in dev. Manual temporary `web-full` copies built both targets; `alder run .../dist/server.mjs -- --port 4344` and Miniflare dispatch of the built Worker returned byte-identical `/probe.bin?v=2`, correct MIME and empty HEAD, with byte-identical copied artifacts.
- [x] Staged web rebuilds remove obsolete public/prerender/target outputs while preserving unrelated dist files; invalid paths and public/prerender collisions preserve the previous build. Direct CLI filesystem tests `rebuild_removes_stale_generated_files_but_keeps_unrelated_dist_files` and `invalid_paths_and_asset_collisions_preserve_last_good_build` pass without subprocesses. Manual rebuilds on both targets after removing a public file and the Grace prerender entry removed the file plus Grace HTML/data while retaining Ada.
- [x] Platform configuration failures become recoverable dev error events; `configuration_failure_is_an_error_event_and_next_build_can_recover` and `public_file_edits_and_removals_change_the_watch_snapshot` pass. Manual Cloudflare dev on 4345 remained HTTP-reachable after an invalid KV resource ID produced the config diagnostic; correcting it emitted Ready and `/health` returned 200 without restarting. This is an HTTP/terminal check, not browser overlay evidence.
- [x] `alder build` emits standalone server and browser artifacts for a routed app. `alder build examples/web-full` emits server/client plus `/about`, `/users/ada`, and `/users/grace` prerender HTML/data.
- [x] The standalone server runs on embedded V8/deno_http without an external app runtime. `alder run examples/web-full/dist/server.mjs -- --port 4346` runs the built artifact directly; the full Chrome acceptance script passes against it.
- [x] `alder build` emits Cloudflare Worker and browser artifacts for the same app. A clean Cloudflare-configured copy emits Worker/client and the same prerender entries. `tools/serve-built-worker.mjs` serves the built artifact directly in Miniflare on 4347; the full Chrome script passes (not the dev bridge).
- [x] Pinned/vendored Miniflare support files power Cloudflare dev directly (no `wrangler dev` or Vite delegation). Native bridge checks and real Chrome dev runs use the pinned compiler support tree. Release packaging/archive/proxy tests verify platform-native dependencies and support placement; shell/PowerShell adapters are pinned to cargo-dist 0.32.0. Local PowerShell execution is delegated to Windows CI, not claimed as locally executed.
- [x] A local Cloudflare runtime smoke check exercises the compiled application and bindings. Built `web-full` browser coverage above exercises the generated Worker/ASSETS; the compiled platform check below exercises native KV, Durable Object storage and Workflow bindings without handwritten adapter classes.
- [x] Cloudflare context bindings, traits/attributes, and generated wrangler.jsonc have direct type/config tests. Driver `cloudflare_traits_produce_solved_adapter_metadata`, `cloudflare_attributes_reject_missing_implementation_and_wrong_binding_type`, `cloudflare_config_all_binding_kinds_are_explicit_and_deterministic`, and `cloudflare_config_rejects_invalid_ids_dates_and_conflicting_names` pass. Kernel `cloudflare_durable_object_initialization_storage_and_binding_isolation`, `cloudflare_queue_decodes_payloads_and_rejects_bad_batches`, and `cloudflare_workflow_checkpoints_keep_codecs_and_provider_context` exercise adapter semantics; real local-platform verification remains its separate gate.
- [x] Durable Object/queue/workflow integration required by the M6 plan is implemented and locally verified. The compiled queue and retained-state DO/Workflow checks below pass in native Miniflare; `cloudflare_owned_native_handle_equality_executes_as_identity` also executes derived wrappers for both opaque state handles, comparing aliases equal and distinct native objects unequal.

Local compiled DO/Workflow check (2026-09-14): temporary Cloudflare app
`/tmp/alder-compiled-platform.mR7VXY` declares actual Alder
`Counter(DurableObjectState)` and `Workflow[Job, Number, Number]` implementations,
plus KV/DO/Workflow binding attributes. `target/debug/alder build
/tmp/alder-compiled-platform.mR7VXY` emitted its Worker and config;
`node /tmp/alder-compiled-platform.mR7VXY/smoke.mjs` instantiated that generated
Worker in native Miniflare with its compatibility flags and configured bindings.
The Counter retains its opaque storage handle from `initObject`, reads a KV
provider inside `fetch`, and increments durable storage on each fetch: 1, 2,
then 3, 4 after complete runtime disposal/recreation with shared persistence.
Native `JOBS.create({params: 41})` ran the compiled durable `step` with the KV
provider and completed with output 42 (local step output `"42"`). Both runtimes
were disposed; no deployment occurred. The temporary harness uses fixed object
and workflow IDs, so reruns must select fresh IDs to repeat its initial-count
assertions without deleting persisted state.

Local queue/prerender check (2026-09-14): a temporary Cloudflare app at
`/tmp/alder-prerender-queue.wbcGF1` contains a static prerendered page and an actual
`#[queue("messages")]` Alder `Queue[Consumer, String]` implementation.
`target/debug/alder build /tmp/alder-prerender-queue.wbcGF1` completed its real
Miniflare build-time render with the configured queue consumer and emitted HTML,
navigation data, Worker, and browser artifacts. Running the temporary
`node /tmp/alder-prerender-queue.wbcGF1/check.mjs` started the built Worker in
Miniflare with the generated compatibility flags, ASSETS binding, and queue
consumer: HTTP `/` byte-matched the generated HTML, and direct queue dispatch
returned `outcome: "ok"`, `retryBatch: {retry: false}`, and no retry messages.
The runtime was disposed. This checks queue-enabled prerender and production
queue dispatch locally, not DO/Workflow coverage or an external deployment.

- [x] `alder deploy` has a real build/config/deploy path with explicit target/environment and secret handling. Two consecutive `target/debug/alder deploy /tmp/alder-m6-production-cloudflare.Zymfos --dry-run` runs passed Wrangler 4.131.2 with identical 207.28 KiB upload / 45.26 KiB gzip packaging and no additional Worker modules. Both prebundled/copied entry files were 212,250 bytes with SHA256 `a6e0d3880a07f6b95ad59277d2f8d92e50a7279d9d029ff610e4dd77db03e392`; 17 static assets were handled separately. Previously contaminated dry-run files were preserved but ignored, with the output count stable at eight. Generated `no_bundle: true` plus `find_additional_modules: false` prevents recursive client/prerender/old-output collection; `cloudflare_prebundled_worker_does_not_scan_adjacent_outputs` and the reviewed full config snapshot lock this policy. Real deploy requires exact Worker/account arguments and uses `--no-bundle --keep-vars --autoconfig=false`; migrations are explicit, credentials remain in Wrangler's auth store/environment. No resource or deployment target is inferred, and neither dry run uploaded a Worker.
- [ ] Real preview deployment: external gate remains open. The user has not supplied an exact preview Worker name/account ID plus authorization and available authentication for that target. No live deployment has been attempted or claimed. Finish local verification first, then request only those missing inputs.

## Final verification and handoff

- [x] Direct compiler/kernel/routing/bundler/runtime regression suite, no Rust CLI or nested-Cargo subprocess tests. Final workspace run includes 731 driver, 142 kernel, 77 codegen, 1,332 parser, and 500 inference tests; all pass.
- [x] Clean-project manual create/dev/edit/error/recovery/navigation/build/run/shutdown workflow in a real browser. Evidence is recorded above for both targets and production artifacts; final dev processes exited 0 and all four test-server ports were released.
- [x] `cargo fmt --all` and `cargo fmt --all --check` pass on the final code.
- [x] `cargo clippy --all-targets --all-features -- -D warnings` passes on the final code.
- [x] `cargo test --quiet` passes for the full workspace, including doctests (two existing ignored doctests). Snapshots reviewed; no `.snap.new` or `.pending-snap` files remain. The final run includes the Cloudflare handle equality canonicalization/linking regressions.
- [x] Docs, examples, changesets, SPEC and this ledger match actual behavior; no unsupported stub is presented as implemented. Scoped final component/error/HMR documentation review corrected the exhaustive server-boundary list. HTML schema regeneration and Node packaging tests pass; Python packaging tests pass with two local PowerShell skips assigned to Windows CI.
- [x] Handoff commands and limitations are documented in `docs/web-development.md` and `examples/web-full/README.md`; this ledger supplies browser/runtime evidence. Live preview verification remains the sole unchecked external gate, not milestone completion. Nothing was committed or pushed.

## Initial authoritative state (2026-09-14)

The worktree contains the uncommitted counter slice, with no routed app/dev/HMR
workflow. Earlier counter verification is historical evidence only; the expanded
implementation must be reverified. Its existing files are preserved as the base.
Parallel bounded work: component renderer expansion, schema/type checking, and
pure routing discovery/type-generation APIs. Root integration owns CLI dev/build,
browser/server entries, platform adapters, and milestone-wide acceptance.
