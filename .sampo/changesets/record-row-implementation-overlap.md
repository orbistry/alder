---
cargo/alder-solve: patch
---

Preserve record row tails when checking overlapping trait implementations,
including shared residual-field constraints across trait arguments.
Match record instance heads during dictionary selection so valid record
implementations can be called, including across module boundaries.
