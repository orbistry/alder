---
cargo/alder-solve: patch
---

Index Option depth constraints by their incident variables instead of rescanning
all constraints for every independent component. Preserve field initializer
context in explicitly annotated lambda record returns.
