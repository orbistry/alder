---
cargo/alder-can: patch
cargo/alder-solve: minor
cargo/alder-codegen: patch
cargo/alder-kernel: patch
---

Expose `Eq` and `Ord` superclass dictionaries through built-in `Hash` and `Num`, and return 64-bit `BigInt` hashes.
